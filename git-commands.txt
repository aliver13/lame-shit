git status // получить инфу по текущему состоянию синхронизации с гитом
git rm <file name>
(git rm filename.js) // удалит файлы из репозитория
git commit //закоммитить изменения, чтобы они отложились в гит
git push origin <repository name>
git push origin main // синхронизирует изменения с гитХабом


NPM

npm install имя_плагина 

mpi i // создаст папку node_modules, в которой окажутся все необходимые модули 


npm nodemon 
автоматически перезапускает сервер за нас, чтобы нам не приходилось это делать каждый раз после каждого
изменения чего-либо

